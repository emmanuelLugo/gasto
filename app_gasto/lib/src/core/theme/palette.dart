import 'package:flutter/material.dart';

class Palette {
  Palette._();

  // static const Color primaryColor = Colors.black;
  static const Color primaryColor = Color.fromRGBO(45, 209, 172, 1);
  static const Color secundaryColor = Color(0XFF8692A4);
  static const Color fontColor = Colors.black54;
}
