import 'package:flutter/material.dart';

class GastoPorClassificacaoPage extends StatelessWidget {
  const GastoPorClassificacaoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Gastos por Telefono'),
      ),
      body: const Column(
        children: [
          Text('GastoPorClassificacaoPage'),
        ],
      ),
    );
  }
}
